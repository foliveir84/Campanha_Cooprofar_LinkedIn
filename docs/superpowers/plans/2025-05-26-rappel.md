# Rappel Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar um desconto de Rappel (máx. 1%) ao motor Cooprofar, aplicando-o sobre o Preço Compra Regular e gerando novas colunas no output e exportação.

**Architecture:** A lógica de cálculo é centralizada em `core/engine/calculator.py` (função pura) e o input/parametrização é gerido em `app.py` via `st.session_state`. Segue o padrão MVC modificado existente (Core puro, UI faz apenas data-binding).

**Tech Stack:** Python 3.9+, Pandas, Streamlit, pytest (novo para testes do core).

---

## File Structure Mapping

| File | Responsibility | Action |
|---|---|---|
| `tests/test_calculator.py` | Testes unitários da lógica de rappel | Create |
| `core/engine/calculator.py` | Recebe `rappel_percent`, computa `Preco_Final_Rappel`, novo critério de vantagem, novo schema de colunas | Modify |
| `app.py` | Widget de input `rappel_percent` na sidebar, validação > 1%, passagem do parâmetro a `evaluate_cooprofar` | Modify |

---

## Task 1: Write Failing Tests for Rappel Logic

**Files:**
- Create: `tests/test_calculator.py`

**Context:** O motor `evaluate_cooprofar` deve aceitar um novo argumento `rappel_percent` (float, default 0.0). Quando > 0, deve:
1. Calcular `Preco_Compra_Regular` (já existe).
2. Calcular `Desconto_Rappel_EUR = round(Preco_Compra_Regular * (rappel_percent / 100), 2)`.
3. Calcular `Preco_Final_Rappel = round(Preco_Compra_Regular - Desconto_Rappel_EUR, 2)`.
4. Filtrar com critério `PVFCampanha < Preco_Final_Rappel`.
5. Calcular diferenças sobre `Preco_Final_Rappel`.
6. O output deve conter as colunas `Desconto_Rappel_EUR` e `Preco_Final_Rappel`.

- [ ] **Step 1: Write the fixtures and failing test**

Crie o ficheiro `tests/test_calculator.py`:

```python
import pandas as pd
import sys
from pathlib import Path

# Ensure project root is on path
sys.path.insert(0, str(Path(__file__).parent.parent))

from core.engine.calculator import evaluate_cooprofar

def test_evaluate_with_zero_rappel_matches_original_logic():
    """Com rappel=0, o resultado deve ser o mesmo logicamente,
    mas com colunas adicionais presentes (Preco_Final_Rappel = Preco_Compra_Regular)."""
    df_template = pd.DataFrame({
        'CNP': [1234567],
        'Designacao_template': ['Paracetamol'],
        'PVF': [10.00],
        'PVFCampanha': [9.00]  # mais barato que regular com desconto
    })
    df_infarmed = pd.DataFrame({
        'Nº registo': [1234567],
        'Preço (PVP)': [6.68],  # Escalão 1
        'Escalao': [1]
    })
    discounts = {1: 8.3}
    result = evaluate_cooprofar(df_template, df_infarmed, discounts, rappel_percent=0.0)

    assert len(result) == 1
    assert 'Preco_Final_Rappel' in result.columns
    assert 'Desconto_Rappel_EUR' in result.columns
    # PVF=10, desconto 8.3%: 10 * (1-0.083) = 9.17
    assert result['Preco_Compra_Regular'].iloc[0] == 9.17
    # Rappel 0 => Final = Regular
    assert result['Preco_Final_Rappel'].iloc[0] == 9.17
    assert result['Desconto_Rappel_EUR'].iloc[0] == 0.00
    # Diferenca = 9.17 - 9.00 = 0.17
    assert result['Diferenca_Absoluta'].iloc[0] == 0.17

def test_evaluate_with_1_percent_rappel_changes_baseline():
    """Rappel de 1% deve incidir sobre o Preco_Compra_Regular."""
    df_template = pd.DataFrame({
        'CNP': [1234567],
        'Designacao_template': ['Paracetamol'],
        'PVF': [10.00],
        'PVFCampanha': [9.10]  # 9.10 < 9.08? Não. Logo NÃO deve aparecer.
    })
    df_infarmed = pd.DataFrame({
        'Nº registo': [1234567],
        'Preço (PVP)': [6.68],
        'Escalao': [1]
    })
    discounts = {1: 8.3}
    result = evaluate_cooprofar(df_template, df_infarmed, discounts, rappel_percent=1.0)
    # Regular = 9.17
    # Rappel = round(9.17 * 0.01, 2) = 0.09
    # Final = 9.08
    # PVFCampanha = 9.10 > 9.08, logo NÃO é vantajoso. Lista deve estar vazia.
    assert len(result) == 0

def test_evaluate_with_rappel_approved_if_campaign_price_below_final():
    """Se PVFCampanha < Preco_Final_Rappel, deve aparecer."""
    df_template = pd.DataFrame({
        'CNP': [1234567],
        'Designacao_template': ['Paracetamol'],
        'PVF': [10.00],
        'PVFCampanha': [9.05]  # 9.05 < 9.08, logo deve aparecer
    })
    df_infarmed = pd.DataFrame({
        'Nº registo': [1234567],
        'Preço (PVP)': [6.68],
        'Escalao': [1]
    })
    discounts = {1: 8.3}
    result = evaluate_cooprofar(df_template, df_infarmed, discounts, rappel_percent=1.0)
    assert len(result) == 1
    assert result['Preco_Final_Rappel'].iloc[0] == 9.08
    assert result['Desconto_Rappel_EUR'].iloc[0] == 0.09
    # Diferenca = 9.08 - 9.05 = 0.03
    assert result['Diferenca_Absoluta'].iloc[0] == 0.03
    assert result['Diferenca_Percentual'].iloc[0] == round((0.03 / 9.08) * 100, 2)
```

- [ ] **Step 2: Run test to confirm failure**

Run:
```bash
pytest tests/test_calculator.py -v
```
Expected: FAIL with `TypeError: evaluate_cooprofar() got an unexpected keyword argument 'rappel_percent'` (ou similar).

- [ ] **Step 3: Commit**

```bash
git add tests/test_calculator.py
git commit -m "test: add failing tests for rappel feature"
```

---

## Task 2: Implement Rappel Logic in `core/engine/calculator.py`

**Files:**
- Modify: `core/engine/calculator.py`

**Context:** A função `evaluate_cooprofar` deve ser expandida. As novas colunas devem estar presentes **sempre**, mesmo que rappel seja 0.0.

- [ ] **Step 1: Update function signature and docstring**

Altere a linha da definição de função em `core/engine/calculator.py`:

```python
def evaluate_cooprofar(df_template: pd.DataFrame, df_infarmed: pd.DataFrame, discounts: dict, rappel_percent: float = 0.0) -> pd.DataFrame:
    """
    Cruza o template da Cooprofar com o Infarmed, aplica os descontos por escalão,
    aplica o desconto de Rappel (se > 0) sobre o Preço Compra Regular,
    e retorna apenas os produtos onde a campanha é matematicamente vantajosa.
    """
```

- [ ] **Step 2: Replace interior logic**

Substitua todo o interior da função `evaluate_cooprofar` (atualmente linhas 64-98 de app.py original / linhas correspondentes do ficheiro) pelo seguinte bloco:

```python
    # --- Merge com Infarmed ---
    df_merged = df_infarmed.merge(df_template, left_on='Nº registo', right_on='CNP', suffixes=('_infarmed', '_template'))
    
    # Mapear descontos
    discounts_clean = {int(k): float(v) for k, v in discounts.items()}
    df_merged['Desconto_Escalao'] = df_merged['Escalao'].map(discounts_clean).fillna(0.0)
    
    # --- Cálculo Regular (escalão) ---
    df_merged['Preco_Compra_Regular'] = df_merged['PVF_template'] * (1 - (df_merged['Desconto_Escalao'] / 100))
    df_merged['Preco_Compra_Regular'] = df_merged['Preco_Compra_Regular'].round(2)
    
    # --- Cálculo Rappel ---
    df_merged['Desconto_Rappel_EUR'] = (df_merged['Preco_Compra_Regular'] * (rappel_percent / 100)).round(2)
    df_merged['Preco_Final_Rappel'] = (df_merged['Preco_Compra_Regular'] - df_merged['Desconto_Rappel_EUR']).round(2)
    
    # --- Filtro de Vantagem (baseline é Preco_Final_Rappel) ---
    df_vantajosos = df_merged[df_merged['PVFCampanha'] < df_merged['Preco_Final_Rappel']].copy()
    
    # --- Diferenças calculadas sobre o Preco_Final_Rappel ---
    df_vantajosos['Diferenca_Absoluta'] = (df_vantajosos['Preco_Final_Rappel'] - df_vantajosos['PVFCampanha']).round(2)
    df_vantajosos['Diferenca_Percentual'] = ((df_vantajosos['Diferenca_Absoluta'] / df_vantajosos['Preco_Final_Rappel']) * 100).round(2)
    
    # --- Renomear e ordenar colunas finais ---
    df_vantajosos['CNP'] = df_vantajosos['CNP'].astype(str)
    
    colunas_finais = {
        'CNP': 'CNP', 
        'Designacao_template': 'Designacao', 
        'Preco_Compra_Regular': 'Preco_Compra_Regular',
        'Desconto_Rappel_EUR': 'Desconto_Rappel_EUR',
        'Preco_Final_Rappel': 'Preco_Final_Rappel',
        'PVFCampanha': 'Preco_Campanha', 
        'Diferenca_Absoluta': 'Diferenca_Absoluta', 
        'Diferenca_Percentual': 'Diferenca_Percentual',
        'PVF_template': 'PVF',
        'Escalao': 'Escalao',
        'Desconto_Escalao': 'Desconto_Escalao',
        'Preço (PVP)': 'Preço (PVP)'
    }
    
    df_finais = df_vantajosos.rename(columns=colunas_finais)
    col_ordem = ['CNP', 'Designacao', 'PVF', 'Escalao', 'Desconto_Escalao', 
                 'Preco_Compra_Regular', 'Desconto_Rappel_EUR', 'Preco_Final_Rappel',
                 'Preco_Campanha', 'Diferenca_Absoluta', 'Diferenca_Percentual', 
                 'Preço (PVP)']
    
    return df_finais[col_ordem].sort_values(by='Diferenca_Absoluta', ascending=False)
```

- [ ] **Step 3: Run tests to confirm green**

Run:
```bash
pytest tests/test_calculator.py -v
```
Expected: 3 tests PASS.

- [ ] **Step 4: Commit**

```bash
git add core/engine/calculator.py
git commit -m "feat: implement rappel logic in evaluate_cooprofar with new schema"
```

---

## Task 3: UI — Widget Rappel na Sidebar e Passagem para o Motor

**Files:**
- Modify: `app.py`

**Context:** Adicionar input de Rappel na sidebar e passá-lo à função. Validar > 1% com warning visual.

- [ ] **Step 1: Add rappel widget to sidebar**

No `app.py`, depois do loop dos descontos (linha 89 aproximadamente) e antes do `# LinkedIn Section`, insira:

```python
    # Separador e Rappel
    st.sidebar.markdown("<hr style='border-top: 1px solid #555; margin: 1rem 0;'>", unsafe_allow_html=True)
    st.sidebar.subheader("Desconto de Rappel (%)")
    
    if 'rappel_percent' not in st.session_state:
        st.session_state['rappel_percent'] = 0.0
    
    rappel_input = st.sidebar.number_input(
        "Rappel (%)",
        value=float(st.session_state['rappel_percent']),
        min_value=0.0,
        step=0.1,
        format="%.2f",
        key="rappel_input_key"
    )
    st.session_state['rappel_percent'] = rappel_input
    
    if rappel_input > 1.0:
        st.sidebar.warning("⚠️ O Rappel não pode exceder 1%.")
```

- [ ] **Step 2: Pass rappel to evaluate_cooprofar**

Altere a chamada a `evaluate_cooprofar` (linha 102-105 aprox):

```python
                df_result_coop = evaluate_cooprofar(
                    df_template_coop,
                    st.session_state['df_infarmed'],
                    st.session_state['discounts'],
                    rappel_percent=st.session_state['rappel_percent']
                )
```

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat: add rappel sidebar widget and wire to evaluate_cooprofar"
```

---

## Task 4: Regression & Smoke Test

**Files:**
- Run: `tests/`

**Context:** Garantir que não houve regressão e que o Streamlit carrega sem erro.

- [ ] **Step 1: Run full pytest suite**

```bash
pytest tests/ -v
```
Expected: Todos os testes PASS. (Até ao momento só existem os 3 testes de rappel; se houver outros, devem continuar a passar.)

- [ ] **Step 2: Smoke test Streamlit**

```bash
python -c "import app; print('Import OK')"
```
Expected: `Import OK` (sem exceções de `SyntaxError` ou `ImportError`).

- [ ] **Step 3: Commit**

```bash
git commit -m "chore: regression and smoke tests pass"
```

---

## Task 5: Final Documentation Tidy

**Files:**
- Already committed: `prdv2.md`, `docs/`

**Context:** O spec já foi escrito. Só verificar se docs/plans está criado.

- [ ] **Step 1: Save this plan to disk**

Este documento deve ser guardado em:
```
docs/superpowers/plans/2025-05-26-rappel.md
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/plans/
git commit -m "docs: add rappel implementation plan"
```

---

## Plan Self-Review

**1. Spec coverage:**
- Sidebar widget com validação > 1%? → Task 3, Step 1 ✅
- Cálculo do rappel sobre preço regular? → Task 2, Step 2 ✅
- Preço Final Rappel como baseline comparação? → Task 2, Step 2 (filtro `PVFCampanha < Preco_Final_Rappel`) ✅
- Novas colunas no output (`Desconto_Rappel_EUR`, `Preco_Final_Rappel`)? → Task 2, Step 2 e Task 1, tests ✅
- Exportação com colunas detalhadas? → Já implementado via `.to_excel` no `df_result_coop` que contém as novas colunas. ✅
- Reactividade automática? → Já garantida pelo modelo Streamlit. ✅

**2. Placeholder scan:**
- Zero TBD, TODO, "implement later". ✅
- Código completo em cada step. ✅

**3. Type consistency:**
- `rappel_percent` é `float` em todos os locais (definição, teste, UI). ✅
- Nome da coluna exportada é `Preco_Final_Rappel` (com F maiúsculo no meio do nome, conforme pedido). ✅

No issues found.
